name: github pages

on:
  watch:
    types: [started]

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
        with:
          submodules: true

      - name: Build Site
        uses: chabad360/hugo-gh-pages@v1.2

      - name: Publish Site
        uses: chabad360/hugo-gh-pages@master
        with:
          githubToken: 95594e8498999f768de7458ef1612892f002a582
          branch: master
          repo: goto10/goto10.github.io
          args: --gc --minify --cleanDestinationDir
